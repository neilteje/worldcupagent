"""
Live arena runner — runs the 4-agent portfolio end-to-end through the World Cup.

  python -m live run        — the forever loop (resumable; safe to kill/restart)
  python -m live once …     — one fixture × window cycle (manual / testing)
  python -m live status     — what's done, what's next
  python -m live report     — retrospective evaluation across all agents
  python -m live test       — connectivity check for every agent key
"""
